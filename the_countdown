#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;
    for (int i = N; i >= 1; --i) {
        cout << i << endl;
        if (i % 5 == 0) {
            cout << "Beep" << endl;
        }
    }
    return 0;
}
